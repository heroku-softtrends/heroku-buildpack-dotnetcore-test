{
  "environments": {
    "test": {
      "formation": {
        "test": {
          "quantity": 1,
          "size": "Standard-1X"
        }
      },
      "buildpacks": [
        {
          "url": "https://github.com/heroku-softtrends/heroku-buildpack-dotnetcore"
        }
      ],
      "scripts": {
        "test-setup": "dotnet '/app/WebApplication2/WebApplication2.dll' >/dev/null 2>&1 & sleep 5",
        "test": "kill $(ps aux | grep 'WebApplication2.dll' | awk '{print $2}')"
      }
    }
  }
}
