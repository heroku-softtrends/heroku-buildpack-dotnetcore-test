{
  "environments": {
    "test": {
      "formation": {
        "test": {
          "quantity": 1,
          "size": "Standard-1X"
        }
      },
      "buildpacks": [
        {
          "url": "https://github.com/heroku-softtrends/heroku-buildpack-dotnetcore"
        }
      ],
      "scripts": {
        "test-setup": "ls -a &  dotnet '/app/WebApplication2/WebApplication2.dll' >/dev/null 2>&1 & sleep 5",
        "test": "ps -ef | grep dotnet | grep -v grep | awk '{print $2}' | xargs kill"
      }
    }
  }
}
