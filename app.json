{
  "environments": {
    "test": {
      "formation": {
        "test": {
          "quantity": 1,
          "size": "Standard-1X"
        }
      },
      "buildpacks": [
        {
          "url": "https://github.com/heroku/heroku-buildpack-google-chrome"
        },
        {
          "url": "https://github.com/heroku-softtrends/heroku-buildpack-dotnetcore"
        }
      ],
      "scripts": {
        "test": "dotnet '/app/WebApplication2/WebApplication2.dll' && kill $(ps aux | grep 'WebApplication2.dll' | awk '{print $2}')"
      }
    }
  }
}
