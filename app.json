{
  "environments": {
    "test": {
      "formation": {
        "test": {
          "quantity": 1,
          "size": "Standard-1X"
        }
      },
      "buildpacks": [
        {
          "url": "https://github.com/heroku-softtrends/heroku-buildpack-dotnetcore"
        }
      ],
      "scripts": {
        "test-setup": "cd \/app/$APP_ASSEMBLY & dotnet --version & dotnet \"./$APP_ASSEMBLY.dll\" 1>/dev/null & sleep 5",
        "test": "ps -ef | grep dotnet | grep -v grep | awk '{print $2}' | xargs kill"
      }
    }
  }
}
