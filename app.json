{
  "environments": {
    "test": {
      "formation": {
        "test": {
          "quantity": 1,
          "size": "Standard-1X"
        }
      },
      "buildpacks": [
        {
          "url": "https://github.com/heroku-softtrends/heroku-buildpack-dotnetcore"
        }
      ],
      "scripts": {
        "test-setup": "export APP_ASSEMBLY=`grep -o '\".*\"' /app/Procfile | perl -pe 's/.\///g' | perl -pe 's/.dll//g'`",
        "test": "echo 'app: $APP_ASSEMBLY'"
      }
    }
  }
}
