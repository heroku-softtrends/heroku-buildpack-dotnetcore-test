{
  "environments": {
    "test": {
      "formation": {
        "test": {
          "quantity": 1,
          "size": "Standard-1X"
        }
      },
      "buildpacks": [
        {
          "url": "https://github.com/heroku-softtrends/heroku-buildpack-dotnetcore"
        }
      ],
      "scripts": {
        "test": "cd $HOME/$PROJECT_NAME && ASPNETCORE_URLS='http://+:$PORT' dotnet './$PROJECT_NAME.dll'"
      }
    }
  }
}
